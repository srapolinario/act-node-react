/*
 * Declarando funções
 * Dado um array de strings retorne um outro array contendo as maiores strings.
 * O array devem conter somente strings do mesmo tamanho e estas devem ter o maior
 * tamanho dentro do array.
 *
 * Faça a declaração da função.
 * Troque "{}" pelo nome dado a função
 */

// code here

type Ex3 = (entrada: string[]) => string[];
export default <Ex3>{};
