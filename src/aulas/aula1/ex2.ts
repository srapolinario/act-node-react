/*
 * Declarando variáveis e entendendo tipos de dados
 * No escopo abaixo existe o código:
 * "export default {}"
 * No contexto atual estamos exportando por padrão o que vier na frente,
 * no caso {}.
 *
 * Faça a declaração de uma variável do tipo boolean definida como falsa
 * Troque "{}" pelo nome dado a variável
 *
 */
const boolean = false;
export default boolean;
